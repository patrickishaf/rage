package validate

import (
	"testing"
)

func TestValdate(t *testing.T) {
	// schema := NewSchema()
	// schema.AddField("name", reflect.TypeOf("name"))

	// type person struct {
	// 	name string
	// }

	// pete := person{
	// 	name: "Pete",
	// }

	// _, err := schema.Validate(pete)

	// if err != nil {
	// 	t.Error("failed to validate")
	// }
}
